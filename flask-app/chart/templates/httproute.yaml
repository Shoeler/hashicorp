apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
spec:
  parentRefs:
  - name: eg
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /secret
    - path:
        type: PathPrefix
        value: /dynamic-secret
    - path:
        type: PathPrefix
        value: /db-query
    - path:
        type: PathPrefix
        value: /pool-status
    backendRefs:
    - name: {{ .Chart.Name }}
      port: {{ .Values.service.port }}
