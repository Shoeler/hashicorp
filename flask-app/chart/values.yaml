replicaCount: 2

image:
  repository: flask-app
  pullPolicy: Never
  tag: "latest"

service:
  type: ClusterIP
  port: 80
  targetPort: 5000

secret:
  name: k8s-secret-from-vault

dynamicSecret:
  name: db-dynamic-creds

postgres:
  host: postgres.default.svc

db:
  poolMin: 2
  poolMax: 10
